type Post {
  id: ID!
  title: String
  content: String!
  blog: ID!
  createdAt: String!
  updatedAt: String!
}

type Blog {
  id: ID!
  name: String!
  slug: String!
  posts: [Post!]!
  createdAt: String!
  updatedAt: String!
}

type Query {
  getBlog(slug: String!): Blog
}